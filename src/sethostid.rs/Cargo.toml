[package]
name = "sethostid"
version = "0.1.0"
authors = ["root"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
crc = "1.8.1"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
